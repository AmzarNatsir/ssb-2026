# === SSO SSB (Identity Provider) — tambahkan ke .env milik ESS ===

# Base URL aplikasi SSB (IdP)
SSB_BASE_URL=http://localhost:8000

# Kredensial client dari admin panel SSB (/admin/sso/clients)
# Dev "ESS (dev)" sudah ada: client_id=1, public/PKCE (tanpa secret)
SSB_CLIENT_ID=1
SSB_CLIENT_SECRET=

# Redirect URI — HARUS sama persis dengan yang didaftarkan di SSB
SSB_REDIRECT_URI=http://localhost:8001/auth/ssb/callback
